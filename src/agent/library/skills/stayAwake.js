//Triggered from modes.js
import { log } from '../log.js';

export async function stayAwake(bot) {
    /**
     * Lets try to stay awake by waking up every 30 minutes.
     **/      
        log(bot, `Stay Awake!`);       
}